const person = {
    name: "hena",
    age: 30,
    friends: ["korim", "hero alom"],
    details: {
        job: "yes",
        isMarrid: true,
        status: "not found"
    }
}

// To access a specific friend, you need an index inside the brackets
console.log(person.friends[0]); // This will print "korim"